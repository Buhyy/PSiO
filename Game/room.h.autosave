#ifndef ROOM_H
#define ROOM_H
#include 

class RoomClass
{
public:
    RoomClass(int type);
    std::vector<std::vector<int>> layout();
    bool is_cleared();
    void set_is_cleared();
private:
    std::vector<std::vector<int>> layout_;
    bool is_cleared_=false;

};

#endif // ROOM_H
